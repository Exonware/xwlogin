# Source size (src/*): ~815.6 KB
[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "xwlogin"
dynamic = [
    "version",
]
description = "Convenience wrapper for exonware-xwlogin - provides 'import xwlogin' alias"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "eXonware Backend Team", email = "connect@exonware.com" },
]
keywords = [
    "oauth2",
    "oidc",
    "identity",
    "login",
    "providers",
    "exonware",
]
classifiers = []
dependencies = [
    "exonware-xwlogin",
]

[project.license]
text = "Apache-2.0"

[project.optional-dependencies]
# First-party XW libs composed by this package (excludes exonware-xwsystem; see lazy/full).
xw = [
    "exonware-xwlogin[xw]",
]
lazy = [
    "exonware-xwlogin[lazy]",
]
full = [
    "exonware-xwlogin[full]",
]
dev = [
    "exonware-xwlogin[dev]",
]

[project.urls]
Homepage = "https://github.com/exonware/xwlogin"
Repository = "https://github.com/exonware/xwlogin"
Issues = "https://github.com/exonware/xwlogin/issues"

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
python_files = [
    "test_*.py",
]
asyncio_mode = "auto"
addopts = [
    "--tb=short",
]
markers = [
    "xwlogin_core: fast core / boundary tests (GUIDE_51)",
    "xwlogin_unit: unit-layer tests (GUIDE_51)",
]

[tool.hatch.version]
path = "src/exonware/xwlogin/version.py"

[tool.hatch.build.targets.wheel]
only-include = [
    "README.md",
    "src/xwlogin_wrapper.py",
]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/README.md",
]
